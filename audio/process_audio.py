def process_audio(file_path: str, target_sr: int = 32000) -> str:
    # TODO Audio Engineer : implémenter avec librosa
    # import librosa, soundfile as sf
    # y, sr = librosa.load(file_path, sr=target_sr)
    # y_trimmed, _ = librosa.effects.trim(y, top_db=20)
    # out_path = file_path.replace('.wav', '_processed.wav')
    # sf.write(out_path, y_trimmed, target_sr)
    # return out_path
    return file_path
